# 🔑 如何設置 Cloudflare API Token

## 方法 1：使用 wrangler login（推薦）

這是最簡單的方法，會打開瀏覽器讓你登錄：

```bash
cd /home/user/webapp
npx wrangler login
```

這會：
1. 打開你的瀏覽器
2. 讓你登錄 Cloudflare
3. 自動保存認證信息

## 方法 2：手動設置 Token

如果方法 1 不行，使用這個：

```bash
# 設置環境變數（只在當前 session 有效）
export CLOUDFLARE_API_TOKEN="你複製的token"

# 測試是否成功
npx wrangler whoami
```

## 方法 3：永久設置（推薦）

```bash
# 在 .bashrc 中添加
echo 'export CLOUDFLARE_API_TOKEN="你複製的token"' >> ~/.bashrc

# 重新加載
source ~/.bashrc

# 測試
npx wrangler whoami
```

## 驗證成功的標誌

運行 `npx wrangler whoami` 後，你應該看到：

```
 ⛅️ wrangler 4.x.x
──────────────────────────────
Getting User settings...
👋 You are logged in with an API Token, associated with the email 'your-email@example.com'.

┌──────────────────────────┬──────────────────────────────┐
│ Account Name             │ Account ID                   │
├──────────────────────────┼──────────────────────────────┤
│ Your Account Name        │ abc123...                    │
└──────────────────────────┴──────────────────────────────┘
```

如果看到這個，說明成功了！✅
